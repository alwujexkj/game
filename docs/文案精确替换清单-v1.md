# 果冻文案精确替换清单 v1

来源：游戏剧情。只改字符串字面量，不改逻辑/分支。按「旧字符串」全文匹配替换更稳（行号仅供定位，以 HEAD 为准）。

涉及：`LevelPlayView.vue`、`SettleModal.vue`、`RoomHubView` / `RoomLobbyView` / `RoomPlayView`、`BagView.vue`、`inventory.ts`、`stickers.ts`（`JELLY_FEED_REACTIONS`）、`packages/shared/src/index.ts`（T1 jelly）、`workshop.ts`；v1.1 补丁另含 Map/Track 可选句。

规范：气泡 ≤12 字优先；用「家人/大家」；低星只庆祝、不评价压力；组队不点名谁错。

---

## 1. `apps/web/src/views/LevelPlayView.vue`

| 旧 | 新 |
|---|---|
| `加油！果冻和你一起闯关～` | `果冻陪你闯关！选一个～` |
| `连击 ×${combo.value}！汪汪！` | `连击 ×${combo.value}！` |
| `答对啦！真棒！` | `答对啦！汪！` |
| `哎呀，再试下一题～` | `再选一次～` |
| `爱心用完了…我们结算吧` | `爱心没了，先看结果～` |
| `配对成功！果冻鼓掌～` | `配对成功！汪！` |
| `再想想，翻开配对的两张～` | `再翻两张试试～` |

（开局句在 ref 初始值与重置处各出现一次，两处都换成同一新句。）

---

## 2. `apps/web/src/components/level/SettleModal.vue`

| 旧 | 新 |
|---|---|
| `` `哇！掉落贴纸「${s?.name ?? props.droppedSticker}」～去背包看看！` `` | `` `贴纸「${s?.name ?? props.droppedSticker}」到手！去背包看～` `` |
| `太棒了！满星通关！汪～` | `满星！门开啦！汪～` |
| `很厉害！再冲一把更高星！` | `两颗星！门开啦！` |
| `过关啦！下次可以更好！` | `过关啦！汪汪～` |
| `没关系，我们再试一次！` | `再来一局？果冻等你～` |

---

## 3. `apps/web/src/views/room/RoomHubView.vue`

| 旧 | 新 |
|---|---|
| `邀请家人输入房间码，一起推开大门！` | `把房间码给家人，一起开门！` |
| `果冻正在准备房间…` | `果冻在开门…` |
| `哎呀，再试一次～` | `再试一次～` |
| `果冻去敲门啦…` | `果冻去找家人啦…` |
| `找不到房间…检查码对不对？` | `码不对？再对一下～` |
| `两台设备怎么玩？` | `几台设备怎么玩？` |
| `3. 双方点准备，房主点开始` | `3. 大家都点准备，房主点开始` |

---

## 4. `apps/web/src/views/room/RoomLobbyView.vue`

| 旧 | 新 |
|---|---|
| `正在连接果冻电台…` | `连上果冻电台…` |
| `正在进入大厅…` | `进大厅啦…` |
| `大家都准备好啦！点开始开门！` | `都好啦！点开始！` |
| `等小伙伴准备…汪汪！` | `等家人准备…汪！` |
| `点「准备好了」等家人一起开门！` | `点「准备好了」，等家人～` |

---

## 5. `apps/web/src/views/room/RoomPlayView.vue`

| 旧 | 新 |
|---|---|
| `` `通关结算！你获得 ${stars} 星～汪！` `` | `` `门开啦！你拿到 ${stars} 星～` `` |
| `答对了！等小伙伴…` | `答对了！等等家人～` |
| `这题有人错了，大家再试一次！` | `这题再一起试试～` |
| `大家一起开门！选对答案～` | `一起选答案，开门！` |
| `果冻：太棒了！门开啦！汪汪～` | `果冻：门开啦！大家真棒！汪～` |
| `有人答错，本题重来` | `这题再一起来～` |
| `等待全员答对…` | `等等家人～` |

---

## 6. `apps/web/src/views/BagView.vue`

| 旧 | 新 |
|---|---|
| `选一张贴纸送给家人，或喂给果冻吧！` | `贴纸送给家人，或喂果冻！` |
| `` `选中「${def.name}」～送给谁呢？` `` | `` `「${def.name}」送给谁？` `` |
| `选中贴纸啦` | `选好贴纸啦` |
| `先点选一张贴纸哦` | `先点一张贴纸～` |

---

## 7. `apps/web/src/stores/inventory.ts`

| 旧 | 新 |
|---|---|
| `背包里没有这张贴纸哦` | `背包里没有这张～` |
| `不能送给自己呀～` | `送给家人或果冻吧～` |
| `` `喂给果冻一张「${sticker?.name ?? stickerId}」` `` | `` `喂果冻：「${sticker?.name ?? stickerId}」` `` |
| `` `已送给家人「${sticker?.name ?? stickerId}」` `` | `` `送给家人啦：「${sticker?.name ?? stickerId}」` `` |

---

## 8. `packages/shared/src/stickers.ts` — `JELLY_FEED_REACTIONS` 整表替换

旧：

```ts
export const JELLY_FEED_REACTIONS = [
  '汪汪！好吃！果冻尾巴摇成螺旋桨～',
  '嗯嗯，再来一根骨头吧！',
  '果冻打了个饱嗝：嗝～谢谢你！',
  '果冻围着你转圈圈，开心到飞起！',
  '竹叶清香～果冻眯起眼睛说：太幸福了！',
] as const;
```

新：

```ts
export const JELLY_FEED_REACTIONS = [
  '汪！好吃！尾巴转圈～',
  '再来一根骨头！',
  '嗝～谢谢你！',
  '果冻围你转圈圈！',
  '真香～果冻眯眼笑！',
] as const;
```

`pickJellyReaction` 仍读同一数组，勿改函数逻辑。

---

## 9. `packages/shared/src/index.ts` — T1 题内 `jelly`（建议同波）

| 旧 | 新 |
|---|---|
| `开门需要 5 块积木！` | `要 5 块积木才能开门！` |
| `门缝打开一点了～` | `门缝开一点啦～` |
| `一起推门！` | （保留） |
| `还差几步！` | （保留） |
| `果冻给你加油！` | `果冻加油！汪！` |

---

## 10. `packages/shared/src/workshop.ts`（建议同波）

| 旧 | 新 |
|---|---|
| `用积木告诉果冻：开门要几分、走几步！` | `开门要几分？果冻走几步？` |
| `矮塔也要加油冲！改题量和题型试试～` | `改题量、题型，冲矮塔～` |
| `点喇叭听一听，再选出正确的卡片！` | `听喇叭，再选对的卡！` |
| `` `开门要 ${cfg.doorScore} 分，果冻先走 ${cfg.jellySteps} 步！` `` | `` `开门 ${cfg.doorScore} 分，走 ${cfg.jellySteps} 步！` `` |
| `` `开门要 ${cfg.doorScore} 分！果冻走 ${cfg.jellySteps} 步～` `` | `` `开门 ${cfg.doorScore} 分！走 ${cfg.jellySteps} 步～` `` |
| `` `最后一题！答对奖励 ${cfg.rewardStars} 星！` `` | `` `最后一题！答对得 ${cfg.rewardStars} 星！` `` |

---

## 11. 可选同波（v1.1）

### `apps/web/src/views/MapView.vue`

| 旧 | 新 |
|---|---|
| `果冻：这个热点还在装修中～` | `果冻：这里还在装修～` |

静态长提示（「点「数学馆」「英语岛」…」）可先改为：`想去哪？果冻带路！`（成长向动态句留给 Wave1）。

### `apps/web/src/views/TrackListView.vue`

| 旧 | 新 |
|---|---|
| `亮着「推荐」的关卡最适合你。也可以挑战其他关卡哦！` | `亮「推荐」的最适合；也可点别的～` |

---

## 明确不改

- 贴纸 `blurb`、关卡 `title` / `subtitle`
- 家长 PIN 说明、排行页现有正向文案
- 成长向动态地图句全表（Wave1）

## 给程序

1. 只改字符串字面量；保留所有 `${…}` 插值变量名。  
2. 优先按「旧」全文匹配；行号仅参考。  
3. `stickers.ts` 改完后确认 `pickJellyReaction` 仍指向该数组。  
4. 本文件即为完整旧→新对照；摘要版已废止。

